<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="WorldeGame.CreditsPage"
             Title="CreditsPage">
    <VerticalStackLayout Spacing="10" VerticalOptions="CenterAndExpand">
        <Label Text="Credits"
           FontSize="24"
           FontAttributes="Bold"
           HorizontalOptions="Center"
           VerticalOptions="StartAndExpand" />

        <StackLayout WidthRequest="500">
            <VerticalStackLayout x:Name="layout">
                <Label Text="Name: "
               FontSize="18"
               FontAttributes="Bold"
               HorizontalOptions="Start"
               VerticalOptions="Start" />
                <Label Text="Michael McDonagh"
               FontSize="16"
               HorizontalOptions="Start"
               VerticalOptions="Start" />
                <Label Text="Email: "
               FontSize="18"
               FontAttributes="Bold"
               HorizontalOptions="Start"
               VerticalOptions="Start" />
                <Label Text="G00421944@atu.ie"
               FontSize="16"
               HorizontalOptions="Start"
               VerticalOptions="Start" />
            </VerticalStackLayout>
        </StackLayout>
    </VerticalStackLayout>
</ContentPage>